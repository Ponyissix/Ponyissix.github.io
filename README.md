# Open Design Pages deployment

This repository hosts a GitHub Pages deployment of the Open Design landing page.

The deployment workflow checks out `Ponyissix/open-design` at `main`, builds
`apps/landing-page`, and publishes the static output to GitHub Pages.
